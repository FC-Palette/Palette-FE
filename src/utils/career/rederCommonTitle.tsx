import { CareerCreateMeetingCommonQuestion } from '@/components/career'

export const renderCareerCommonTitle = (text: string) => {
  return (
    <CareerCreateMeetingCommonQuestion>
      {text}
    </CareerCreateMeetingCommonQuestion>
  )
}
